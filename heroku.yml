build:
  docker:
    web: Dockerfile
run:
  web: /bin/bash -c "envsubst '\$PORT' < /etc/nginx/sites-available/default.template > /etc/nginx/sites-available/default" && supervisord -n -c /app/supervisord.conf