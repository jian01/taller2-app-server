build:
  docker:
    web: Dockerfile
run:
  web: /bin/bash -c "envsubst '\$PORT' < /etc/nginx/sites-available/appserver.template > /etc/nginx/sites-available/appserver" && supervisord -n -c /app/supervisord.conf