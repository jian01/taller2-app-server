build:
  docker:
    web: Dockerfile
run:
  web: bin/start-nginx gunicorn -k sync 'create_application:create_application("config/deploy_conf.yml")' --log-config config/logging_conf.ini --timeout 60 --bind unix:nginx.socket